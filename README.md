# connect-disconnect-touchpad
script to disconnect or disable touchpad when optical mouse is connected and reverse

### execute into folder like root:
cp mymouse.sh /lib/udev & cp 85-mymouse.rules /etc/udev/rules.d
